---
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill]
tags: [smoke]
---

I'm building an API with django-modern-rest and msgspec is installed.
Write the Python module for a `POST /users/` endpoint that accepts
a JSON body with `email` (string) and `age` (integer) and responds
with the created user and a 201 status code. Show the complete module.
