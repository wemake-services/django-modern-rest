# Upgrade django-modern-rest from 0.9.0 to 0.10.0

Source: CHANGELOG.md, release 0.10.0 (2026-05-26).
Breaking changes for this release are listed in `CHANGELOG.md` under `0.10.0`.

Read this file only while upgrading across this release. Apply every item,
search the whole project (including tests and fixtures) for each one.

## Prompt

Change all existing `dmr.files.FileResponseSpec` usages
to include `as_attachment=True` parameter.
