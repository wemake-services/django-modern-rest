version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"

python:
  install:
    - method: uv
      command: sync
      extras: all
      groups: ["docs"]
      path: .

sphinx:
  configuration: 'docs/conf.py'
  fail_on_warning: true
