version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: '3.13'

python:
  install:
    - method: uv
      command: sync --no-emit-project
      extras: all
      groups:
        - docs

sphinx:
  configuration: 'docs/conf.py'
  fail_on_warning: true
